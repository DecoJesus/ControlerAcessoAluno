@model IEnumerable<ControlerAcessoAluno.Models.TB_TURMA>

@{
    ViewBag.Title = "Index";
}

<h2></h2>

<p>
    @Html.ActionLink("Criar uma Nova Turma", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SERIE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PERIODO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HORA_ENTRADA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HORA_SAIDA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_CURSO.NOME_CURSO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SERIE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PERIODO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HORA_ENTRADA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HORA_SAIDA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_CURSO.NOME_CURSO)
        </td>
        <td>
            @Html.ActionLink("EDITAR", "Edit", new { id=item.COD_TURMA }) |
            @Html.ActionLink("DETALHE", "Details", new { id=item.COD_TURMA }) |
            @Html.ActionLink("REMOVER", "Delete", new { id=item.COD_TURMA })
        </td>
    </tr>
}

</table>
