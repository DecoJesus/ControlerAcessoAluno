@model IEnumerable<ControlerAcessoAluno.Models.TB_CARGO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/admin.cshtml";
}

<h2></h2>

<p>
    @Html.ActionLink("CrIAR NOVO CARGO", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CARGO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CARGO)
        </td>
        <td>
            @Html.ActionLink("EDITAR", "Edit", new { id=item.COD_CARGO }) |
            @Html.ActionLink("DETALHE", "Details", new { id=item.COD_CARGO }) |
            @Html.ActionLink("REMOVER", "Delete", new { id=item.COD_CARGO })
        </td>
    </tr>
}

</table>
